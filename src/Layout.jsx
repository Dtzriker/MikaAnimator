import React from 'react';
export default function Layout({ children }) {
  return <div style={{ height: '100vh', overflow: 'hidden', background: '#08081A', fontFamily: 'Inter, system-ui, sans-serif' }}>{children}</div>;
}

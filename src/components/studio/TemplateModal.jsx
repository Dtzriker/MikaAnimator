import React from 'react';
import { X } from 'lucide-react';
import { TEMPLATES } from '../engine/templates';
export default function TemplateModal({ onSelect, onBlank, onClose }) {
  return <div style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.85)', backdropFilter: 'blur(12px)', zIndex: 1000, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 24 }}><div style={{ background: '#0D0D1F', border: '1px solid rgba(255,255,255,0.1)', borderRadius: 20, width: '100%', maxWidth: 680, padding: 32, position: 'relative', boxShadow: '0 40px 100px rgba(0,0,0,0.8)' }}><button onClick={onClose} style={{ position: 'absolute', top: 16, right: 16, background: 'rgba(255,255,255,0.07)', border: 'none', borderRadius: 8, cursor: 'pointer', color: '#fff', padding: 7 }}><X size={16} /></button><div style={{ textAlign: 'center', marginBottom: 28 }}><div style={{ fontSize: 36, marginBottom: 8 }}>🧱</div><h2 style={{ color: '#fff', fontSize: 24, fontWeight: 800, fontFamily: 'Inter, sans-serif', letterSpacing: '-0.03em', margin: 0 }}>MotionClay</h2><p style={{ color: 'rgba(255,255,255,0.4)', fontSize: 14, marginTop: 6, fontFamily: 'Inter, sans-serif' }}>Start with a template or build from scratch</p></div><div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 12, marginBottom: 16 }}>{TEMPLATES.map(t => <button key={t.id} onClick={() => onSelect(t)} style={{ background: 'rgba(255,255,255,0.04)', border: '1px solid rgba(255,255,255,0.09)', borderRadius: 14, padding: '16px', cursor: 'pointer', textAlign: 'left' }}><div style={{ width:'100%', height:80, borderRadius:8, background:t.bg, marginBottom:12, display:'flex', alignItems:'center', justifyContent:'center', fontSize:28 }}>{t.emoji}</div><p style={{ color:'#fff', fontWeight:700, fontSize:13, margin:'0 0 4px' }}>{t.name}</p><p style={{ color:'rgba(255,255,255,0.4)', fontSize:11 }}>{t.description}</p></button>)}</div><button onClick={onBlank} style={{ width: '100%', padding: '14px', background: 'transparent', border: '1px dashed rgba(255,255,255,0.2)', borderRadius: 12, color: 'rgba(255,255,255,0.5)', fontSize: 13, fontWeight: 600, cursor: 'pointer' }}>+ Start with Blank Canvas</button></div></div>;
}
